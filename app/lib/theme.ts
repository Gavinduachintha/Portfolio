/**
 * Design-system colour tokens.
 *
 * Use these constants instead of hardcoding hex values so that a brand-colour
 * change only requires editing this one file.
 *
 * CSS custom properties (--accent, --accent-dim, etc.) are defined in
 * globals.css and should stay in sync with the values here.
 */

export const ACCENT       = "#4fda8e"; // primary green accent
export const ACCENT_DIM   = "#3bb36d"; // darker variant used for hover states
export const ACCENT_DARK_TEXT = "#052e1f"; // dark text placed on top of ACCENT backgrounds
